"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
var buildClassName = exports.buildClassName = function buildClassName(element, options, additionalClasses) {
  var selectedOptions = Object.keys(options).filter(function (option) {
    return options[option] === true;
  });
  var classNames = selectedOptions.map(function (option) {
    return element + "-" + option;
  }).join(" ");
  return element + " " + classNames + " " + additionalClasses;
};